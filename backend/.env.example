# =========================================================
# 0) 必填基础配置（先改这几项，项目就能跑起来）
# =========================================================

# 项目通用 API_KEY（目前用于配置校验，任意非空字符串即可）
API_KEY=change_me

# 数据库连接（默认本地 SQLite）
DB_URL=sqlite+aiosqlite:///./app.db

# TwitterAPI.io 的 API Key（抓取推文必填）
TWITTERAPI_IO_API_KEY=your_twitterapi_io_key

# 智谱 GLM API Key（需要 AI 分析时必填）
ZAI_API_KEY=your_zhipu_api_key


# =========================================================
# 1) 抓取相关（一般保持默认）
# =========================================================

# TwitterAPI.io 服务地址（通常不需要改）
TWITTERAPI_IO_BASE_URL=https://api.twitterapi.io

# Demo 用用户名（仅示例脚本使用）
TWITTERAPI_IO_DEMO_USERNAME=KaitoEasyAPI

# 关键字模式：回看最近 N 小时（默认 24）
KEYWORD_LOOKBACK_HOURS=24

# 关键字模式：最低点赞阈值（越大越“精选”）
KEYWORD_MIN_LIKES=30

# 作者模式：每次最多抓取条数
AUTHOR_FETCH_LIMIT=10


# =========================================================
# 2) LLM 调用相关（先用默认，按成本和稳定性再调）
# =========================================================

# 默认模型名
GLM_MODEL=glm-4.5-air

# 单次请求超时时间（秒）
GLM_TIMEOUT_SECONDS=30

# 失败重试次数（建议 1~3）
GLM_MAX_RETRIES=2

# 批量分析时每批大小（越大越省请求，但单次更重）
LLM_ANALYZE_BATCH_SIZE=8


# =========================================================
# 3) 可选：飞书直连 demo（仅 demo_send_feishu.py 使用）
# =========================================================

# 飞书群机器人 webhook（不用可留空）
FEISHU_WEBHOOK_URL=

# 飞书机器人签名密钥（不用可留空）
FEISHU_BOT_SECRET=


# =========================================================
# 4) 应用信息（一般保持默认）
# =========================================================

# 应用时区（影响展示时间与任务时间基准）
APP_TIMEZONE=Asia/Shanghai

# 应用名（日志显示）
APP_NAME=AI Tech Aggregator Backend

# 运行环境标识：development / production
APP_ENV=development
