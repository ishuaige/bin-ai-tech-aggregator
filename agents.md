# AI 全栈工程师索引 (Index Only)

## 1. 角色定位
你是一个全栈开发专家。项目根目录下的 `.docs/` 目录存放着本项目的所有规格说明。

## 2. 上下文读取规则 (CRITICAL)
- **默认状态**：不要主动读取 `.docs/` 下的所有文件以节省 Token。
- **触发读取**：当我提到“根据需求”、“根据设计”或使用 `@` 符号指定文档时，请务必精准阅读相关文档。
- **项目边界**：
  - 后端: `backend/` (FastAPI + uv)
  - 前端: `frontend/` (Vue 3 + Vite + Node 22)

## 3. 核心红线 (不读文档也必须遵守)
- 严禁在 `async` 函数中使用同步阻塞代码。
- 前端组件必须使用 `<script setup>` 且必须带 `scoped` 样式。
- 所有的 Python 函数必须标注类型提示 (Type Hints)。

## 4. 断点续传 (新对话专用)
如果这是我们的第一次对话，请直接回复：“Ready”，无需读取文档。等到我下达具体任务并指定文档时再开始工作。